# @package config.monty_config

defaults:
  - motor_system: naive_scan_spiral
  - learning_module/displacement@learning_module_configs.learning_module_0
  - sensor_module: sm0_twodsm_sm1_probe
  - connectivity: 1lm

monty_class: ${monty.class:tbp.monty.frameworks.models.graph_matching.MontyForGraphMatching}
monty_args:
  num_exploratory_steps: 1000
  min_eval_steps: 3
  min_train_steps: 3
  max_total_steps: 2500
