# Base EvidenceGraphLM config with ??? for required sub-components.
# Concrete configs (evidence_distant, evidence_distant_hsv_only) fill in
# gsg, hypotheses_updater_args, feature_weights, and tolerances.

learning_module_class: ${monty.class:tbp.monty.frameworks.models.evidence_matching.learning_module.EvidenceGraphLM}
learning_module_args:
  max_match_distance: 0.01
  x_percent_threshold: 20
  evidence_threshold_config: 80%
  max_graph_size: 0.3
  num_model_voxels_per_dim: 100
  gsg: ???
  hypotheses_updater_args: ???
  feature_weights: ???
  tolerances: ???
