# @package _global_

defaults:
  - /experiment/2d_sm/inference/disk_inference_control_on_control

experiment:
  config:
    logging:
      run_name: disk_inference_control_20pos_burst_sampling
    monty_config:
      learning_modules:
        learning_module_0:
          _target_: tbp.monty.frameworks.models.detailed_logging_lm.EvidenceGraphLMWithDetailedLogging
          hypotheses_updater_class: ${monty.class:tbp.monty.frameworks.models.evidence_matching.burst_sampling.BurstSamplingHypothesesUpdater}
          hypotheses_updater_args:
            max_nneighbors: 10
            deletion_trigger_slope: 0.0
            burst_trigger_slope: 0.7
            include_telemetry: true
          max_match_distance: 0.01
          max_graph_size: 0.3
          num_model_voxels_per_dim: 100
          tolerances:
            patch:
              hsv: ${np.array:[0.1, 0.2, 0.2]}
          feature_weights:
            patch:
              hsv: ${np.array:[1, 0.5, 0.5]}
              pose_vectors: ${np.array:[0, 2, 1]}
          x_percent_threshold: 20
          evidence_threshold_config: all
          object_evidence_threshold: 10
          gsg:
            _target_: tbp.monty.frameworks.models.goal_generation.EvidenceGoalGenerator
            goal_tolerances:
              location: 0.015
            elapsed_steps_factor: 10
            min_post_goal_success_steps: 5
            x_percent_scale_factor: 0.75
            desired_object_distance: 0.03
