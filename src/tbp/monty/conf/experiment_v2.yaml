# @package _global_

defaults:
  - _self_
  - benchmarks: defaults
  - logging: defaults
  - dataset: disk_objects
  - monty: patch_and_view_inference
  - environment: habitat
  - mode: inference
  - hydra: defaults
  # Experiment config (required) - use override to change defaults above
  - experiment: MISSING

# Global settings
config:
  episodes: all
  num_parallel: 16
  print_cfg: false
  quiet_habitat_logs: true
